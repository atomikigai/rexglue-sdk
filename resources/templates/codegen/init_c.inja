// Generated by rexglue
// DO NOT EDIT - this file is auto-generated
// C11 variant of {project}_init.cpp: no rex:: namespace, no extern "C" (C
// already has C linkage), nullptr becomes NULL.

// REXCRT_HEAP and the REX_CONFIG_* flags (config_flags below) are already
// compile-time macros defined in {{ project }}_pch.h (see pch_h_c.inja); the
// C backend's PPCImageInfo (x360rt/ppc.h) carries only the fields the
// runtime actually consumes at load time (image/code range, dispatch table,
// mapping count). Per-module DLL registration (has_dll_modules && !is_dll)
// is handled by the app: module_registry.{{ source_ext }}'s
// RegisterRecompiledModules() is called directly by x360_app_run once this
// module's own image is registered, instead of being threaded through this
// struct.
#include "{{ project }}_init.h"

const PPCImageInfo PPCImageConfig = {
    .code_base = REX_CODE_BASE,
    .code_size = REX_CODE_SIZE,
    .image_base = REX_IMAGE_BASE,
    .image_size = REX_IMAGE_SIZE,
    .func_mappings = PPCFuncMappings,
    .func_mapping_count = {{ func_mapping_count }}u,
    .flags = 0u,
};

PPCFuncMapping PPCFuncMappings[] = {
{% for fn in functions %}{% if not fn.below_code_base or fn.is_import %}	{ {{ fn.address }}, {{ fn.name }} },
{% endif %}{% endfor %}	{ 0, NULL }
};
